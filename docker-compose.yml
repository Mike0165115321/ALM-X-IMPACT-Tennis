services:
  # 🐍 บริการระบบหลังบ้าน FastAPI
  backend:
    build: ./backend
    container_name: backend-tennis
    ports:
      - "8000:8000"
    volumes:
      - ./backend/uploads:/app/uploads # เมาท์โฟลเดอร์สำหรับเก็บภาพสลิปจริงลงโฮสต์
    env_file:
      - ./backend/.env # โหลดตัวแปรสภาพแวดล้อมโดยตรง
    networks:
      - tennis-network
    restart: always

# 🌐 เครือข่ายแยกอิสระเพื่อความปลอดภัย
networks:
  tennis-network:
    driver: bridge
